generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./database/database.db"
}

model Games {
  id String @id @default(uuid())

  name String @unique
  description String
  developer String

  @@map("games")
}
