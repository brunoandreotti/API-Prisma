generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Games {
  id String @id @default(uuid())

  name String @unique
  description String
  developer String

  @@map("games")
}
